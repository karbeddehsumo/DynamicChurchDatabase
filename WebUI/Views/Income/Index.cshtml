

@{
    ViewBag.Title = "Index";
    
}
<div>
<h2 id="pageTitle" class="center">Income Center</h2>

    <div class="center">
        @Ajax.ActionLink("Add Income", "Create", "Income", new { }, new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "IncomeIndexPane" }, new {@id="pageSubTitle"})
        @if (@ViewBag.Supervisor == true)
        {
        @Ajax.ActionLink("View Income", "Details", "Income", new {}, new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "IncomeIndexPane" }, new {@id="pageSubTitle"})
        }
    </div>
    </div>
<div id="IncomeIndexPane">
@Html.Action("Details", "Income", new {})
</div>


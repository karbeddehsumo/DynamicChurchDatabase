@model IEnumerable<Domain.programevent>

@{
    ViewBag.Title = "Index";
    
}



<h2 class="center-text">Event Center</h2>
<br />

    <div>
        @Ajax.ActionLink("Add Event", "Create", "ProgramEvent", new {}, new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "RightPane" })
        @Ajax.ActionLink("View Events", "Details", "ProgramEvent", new {}, new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "RightPane" })
    </div>

<div id="RightPane">
@Html.Action("List", "ProgramEvent", new {})
</div>

<script type="text/javascript">

    $(document).ready(function () {
        $('.datepicker').not('.hasDatePicker').datepicker();

    });

 </script>
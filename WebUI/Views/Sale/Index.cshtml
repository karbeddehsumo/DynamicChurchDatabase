@model IEnumerable<Domain.sale>

@{
    ViewBag.Title = "Index";
    
}



<h2 class="center-text">Sale Center</h2>
<br />

    <div>
        @Ajax.ActionLink("Add Sale", "Create", "Sale", new {}, new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "RightPane" })
        @Ajax.ActionLink("View Sales", "Details", "Sale", new {}, new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "RightPane" })
    </div>

<div id="RightPane">
@Html.Action("List", "Sale", new {})
</div>

<script type="text/javascript">

    $(document).ready(function () {
        $('.datepicker').not('.hasDatePicker').datepicker();

    });

 </script>